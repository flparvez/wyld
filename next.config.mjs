/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', 'aceternity.com', 'daisyui.com', 'images.unsplash.com', 'assets-global.website-files.com', "localhost"]
    }
};

export default nextConfig;
