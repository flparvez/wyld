/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', 'aceternity.com', 'daisyui.com', 'images.unsplash.com', 'assets-global.website-files.com', "localhost", 'i.ibb.co']
    }
};

export default nextConfig;
